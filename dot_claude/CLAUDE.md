# グローバル設定

## 回答スタイル
- 挨拶・前置き・絵文字禁止。結論ファースト
- 直前の作業内容の再説明・末尾まとめは不要（差分を読めばわかる）
- 短く端的に。一文で済むなら一文にする

## git 操作
- push は明示的に指示があった場合のみ実行
- commit は明示的に指示があった場合のみ作成
- 破壊的操作（reset --hard, branch -D 等）は必ず事前確認

## 言語
- 基本的に日本語で回答する
