[ghq]
	root = ~/src
[user]
	name = mikamo3
	email = kamo3proj@gmail.com

[alias]
	lol = log --oneline
	st = status
	co = checkout
  sp = stash pop
  ss = stash save
	cob = checkout -b
	bd = !git branch -r --merged master | grep -v -e master -e develop | sed -e 's% *origin/%%' | xargs -I% git push --delete origin %
