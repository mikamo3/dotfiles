#!/usr/bin/env bash

WORK_DIRS=($(ghq list -p))
selected="$(
  for ((i = 0; i < ${#WORK_DIRS[@]}; i++)) do
    echo "${WORK_DIRS[$i]}"
  done | rofi -dmenu -i -p workdirectory
)"

if [[ $? == 0 ]];then
  code --folder-uri ${selected}
  exit
fi